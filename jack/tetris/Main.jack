/** Initializes a new Tetris game and starts running it. */
class Main {
    function void main() {
        var Tetris tetris;
        let tetris = Tetris.new();
        do tetris.run();
        do tetris.dispose();
        return;
    }
}
